# Flutter / Dart build + tooling artefacts (regenerated locally)
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
.pub-cache/
.pub/

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/

# Generated platform folders are created locally with `flutter create .`
# (kept out of git so the repo stays lean — see README).
/android/
/ios/
/linux/
/macos/
/windows/
/web/

# Local run script holding the Google Places API key (never commit the key).
# Copy run.sh.example -> run.sh and paste your key in run.sh.
run.sh

# Misc
*.log
.DS_Store
