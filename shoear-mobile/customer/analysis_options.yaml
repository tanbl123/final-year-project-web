# Standard Flutter lint set (https://pub.dev/packages/flutter_lints).
include: package:flutter_lints/flutter.yaml

linter:
  rules:
    # allow `print`-free debugging via the framework; keep defaults otherwise.
